{% extends 'base.html.twig' %}

{% block body %}
    <div style="margin-top: 80px"></div>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <button type="submit">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
